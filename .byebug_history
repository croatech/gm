exit
Sequel.connect("postgres:password//localhost:5435/movies")
exit
Sequel.connect("#{ENV['POSTGRES_USER']}:#{ENV['POSTGRES_PASSWORD']}//#{ENV['POSTGRES_HOST']}:#{ENV['POSTGRES_PORT']}/#{ENV['POSTGRES_DATABASE']}")
Sequel.connect("#{ENV['POSTGRES_USER']}:#{ENV['POSTGRES_PASSWORD']}//#{ENV['POSTGRES_HOST']}:#{ENV['POSTGRES_PORT']}/movies")
Sequel.connect("#{ENV['POSTGRES_USER']}:#{ENV['POSTGRES_PASSWORD']}//#{ENV['POSTGRES_HOST']}:#{ENV['POSTGRES_PORT']}/#{ENV['POSTGRES_DATABASE']}")
"#{ENV['POSTGRES_USER']}:#{ENV['POSTGRES_PASSWORD']}//#{ENV['POSTGRES_HOST']}:#{ENV['POSTGRES_PORT']}/#{ENV['POSTGRES_DATABASE']}"
ENV
ENV | grep 'POSTGRES'
ENV | grep POSTGRES
ENV
